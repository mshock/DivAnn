%h1= @server.nil? ? 'Tables' : "Tables for #{@server.name.upcase}"
.server_select
  %select{:name => 'feed', :id => 'feed_select2'}
    - @feeds.each do |feed|
      %option{:value => "#{feed.id}"}= feed.name.upcase
%br
= haml :tables_partial, :layout => false