%h1 Feeds
.server_select
  %select{:name => 'server', :id => 'server_select2'}
    - @servers.each do |server|
      %option{:value => "#{server.id}"}= server.name.upcase
%br
= haml :feeds_partial2, :layout => false